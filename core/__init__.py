"""Notchstave shared core: database models, enums, common helpers.

This package intentionally contains no business logic — only what every
process (bot / api / deriver / watcher / settler / notifier) needs to agree on.
"""
